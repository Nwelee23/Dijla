# AGENTS.md — Project Memory

> Place this file at the **root of the repo**. Agent IDEs read it as standing instructions every session,
> so the agent always knows what this project is, the stack, and the conventions. Keep it updated as the app grows.
>
> **Antigravity note:** Antigravity natively reads `AGENTS.md` (v1.20.3+) and `GEMINI.md`. If your Antigravity
> version is older, also save a copy of this file as `GEMINI.md` in the repo root. (`GEMINI.md` takes precedence if both exist.)

## Project

**Name:** Dijla (دجلة) — *the Tigris river; deeply Iraqi, evokes flow and continuity.*

**What it is:** A commission-free ordering & operations platform (SaaS) for Iraqi restaurants, built for Najaf first.
One system, three surfaces:
1. **Customer** — QR dine-in menu (`/t/[qr_token]`) + direct delivery link (`/r/[slug]`). No app install.
2. **Restaurant dashboard** (`/dashboard`) — live orders, menu, tables/QR, drivers, reports.
3. **Driver app** (`/driver`) — assigned deliveries, navigate to GPS pin, status updates.

**Business model:** flat monthly subscription per restaurant. **Never per-order commission** — that's the whole pitch vs. Talabat/Baly.

**Key product rules:**
- Delivery addresses in Iraq are informal → capture a **GPS pin** from the customer's browser + a "nearest landmark" text field. Do NOT rely on street addresses.
- **COD (cash on delivery)** is the only payment for now. No payment gateway yet.
- Ship order: dine-in first, then delivery, then driver dispatch.

## Stack

- **Framework:** Next.js (App Router) + TypeScript
- **Styling/UI:** Tailwind CSS + shadcn/ui. **RTL + Arabic-first**, mobile-first.
- **Backend/DB/Auth/Realtime/Storage:** Supabase (PostgreSQL). Use Supabase Realtime for live orders and Supabase Storage for images.
- **Auth:** Supabase Auth — **email OTP (primary)**, delivered via **Brevo SMTP** configured in the Supabase dashboard. Email/password is the fallback. Phone OTP is coded but dormant: it needs a paid SMS provider, so switch it on only when the pilot justifies the cost.
- **Maps:** browser Geolocation API + Leaflet/OpenStreetMap; navigation via Google Maps deep-link. Avoid paid Maps APIs early.
- **QR:** `qrcode` npm package.
- **Hosting:** Vercel (frontend) + Supabase (backend). PWA installable (manifest + service worker), like the previous `dealshop-iq` app.

## Conventions

- **No emoji in the product. Ever.** Every glyph in the UI is a `lucide-react` icon — no emoji in labels, buttons, empty states, toasts, or seed data. Emoji render differently on every device, break alignment in RTL, and read as unprofessional to a restaurant owner. This applies to shipped UI; commit messages and chat are unaffected.
- **Multi-tenant:** almost every table has `restaurant_id`. Enforce isolation with Supabase **Row-Level Security (RLS)** — a user only accesses rows for their own restaurant. Drivers only see orders assigned to them.
- **Never hardcode secrets.** Use `.env.local`; keep the Supabase **service_role** key server-side only.
- **Types:** generate DB types from Supabase; keep shared types in `lib/supabase/`.
- **Money:** store as numeric; currency default `IQD`.
- **Snapshots:** `order_items` store `name_snapshot` and `price_snapshot` so past orders don't change when the menu changes.
- **Roles:** `owner` | `staff` | `driver` | `admin` (in `profiles.role`).
- Keep components small; put data hooks in `lib/hooks/` (e.g. `useRealtimeOrders`, `useCart`, `useGeolocation`).

## Current status

- **Phases 0–5 complete + deployed**, plus Phase 7's growth-metrics layer. Built: foundations, menu engine, dine-in QR ordering, delivery + driver dispatch, driver app, reports, subscriptions, a super-admin panel, and the founder growth dashboard (`/admin/growth`).
- **AUTH_UI_SPEC.md fully built (5/5)** — replaces the old tasks 1.1–1.2. River-night glass design system; username+password login; emailed-code sign-in; forgot/reset (invalidates other sessions); 3-step signup wizard (restaurant fields + Leaflet GPS pin + **private** `verification-docs` bucket + live username availability); and pending→verified gating (a pending restaurant builds its menu but `/api/orders` refuses `not_verified`; a guard trigger blocks owner self-verify).
- **Auth model deviation from the spec:** there is no SMS provider, so **email is the verify/recovery anchor** (not phone OTP), chosen by the owner. Phone-OTP code stays wired behind a switch. Daily login is username+password (username→account email resolved server-side). Working hours are left 24h-open for now; logo is added later in settings.
- **ORDERS_DASHBOARD_SPEC.md fully built (8/8)** — supersedes task 2.6. Live 3-column kanban (new/preparing/ready); live per-card elapsed timer with per-restaurant warn/danger thresholds (in `settings` jsonb, Settings-configurable); 10-second undo on every status change (compensating history row); cancel-with-reason + confirm step (`orders.cancellation_reason`); day archive at `/dashboard/orders/archive` (never delete — completed/cancelled leave the board); kitchen screen as its own route `/dashboard/orders/kitchen` (big type, no prices, one action, Screen Wake Lock); new-order alert hardened (20s repeat while unacknowledged + tab-title count badge); reconnect-resync confirmation toast. **The whole restaurant dashboard is now river-night dark** (`.dark.dj-dashboard` scope in globals.css, teal-tinted, with a `@media print` light reset for QR/cash sheets; contrast verified ≥4.5:1). Delivery detail (§7) was already on the card (tap-to-call, landmark, map, Navigate, no PII in URLs).
- **MENU_BUILDER_SPEC.md built** — supersedes tasks 1.5/1.6/2.2/2.3. Starter-menu templates per restaurant_type (import from the empty state, `src/lib/menu-templates.ts`); duplicate item with its option groups/options (no shared image file); menu preview (opens `/r/[slug]`), completeness nudge and live count in the header; `menu_items.name_secondary`/`description_secondary` (migration 0024) with an optional second-language name field; full option-groups editor in the item dialog (add group, required/multi = max_select 1/99, options with +price deltas, immediate-persist); tables/QR gained the public delivery-link copy and a white QR backing so codes stay scannable on the dark theme (print A4 / per-table PNG / nanoid tokens already existed). §5 friction rules hold: name+price is enough to save, category auto-defaults, photos never required. (Photo-to-menu extraction §2.3 deferred by the spec; a minor "N sizes · N extras" row-meta count is still unbuilt.)
- **DRIVER_REPORTS_ADMIN_SPEC.md applied** — supersedes tasks 4.4/4.5/4.8 and 5.1/5.2/5.4/5.5. The driver app and admin panel gained the **river-night dark theme** (`.dark dj-dashboard` on their own layouts). Admin: **churn early-warning** — a restaurant with no orders for 7+ days is flagged "quiet for Nd", dimmed, and filterable (measured from last order, or signup for one that never ordered); and a **verification review dialog** showing owner/landmark/intent-signal (signup age + menu items built) with ID/licence documents opened via **short-lived signed URLs** from the private bucket (never public), verify/reject-with-reason inside. Driver: **"cash in hand"** running total (today's delivered cash) + today's count in the persistent sticky header, on every screen (§A.5), RLS-scoped to the driver. Driver realtime+sound alert, oversized Navigate/Call, editable cash confirmation, and the server-route write-guard (`driver_id = auth.uid()`, whitelisted columns) were already built in Phase 4; reports (KPIs incl. prep time, peak hours, top items, type split, per-driver cash, all server-aggregated) in Phase 5. No new migration. Security re-verified: driver isolation (can't read/modify another's order, can't write the order row directly), non-admins get no admin data, verification docs never public.
- **REMAINING_SCREENS_SPEC.md applied** — supersedes tasks 3.2/3.3, extends 4.1, covers 1.7/2.8/5.10. Built the **marketing landing page** at `/` (dark river-night, seven sections, an **interactive savings calculator** that reframes the subscription as a saving vs ~25% delivery-app commission, `src/lib/pricing.ts` config, WhatsApp CTA via `NEXT_PUBLIC_SUPPORT_PHONE`, Arabic OG tags) and a **"temporarily closed" switch** in Settings (`settings.closed_now`, folded into `getOpenState` so the existing order gate + customer menu honour it). The customer delivery checkout (three location states, landmark-**or**-pin submit rule so GPS never blocks an order — §A.2.1, verified server-side), live 4-step order tracking, drivers/settings/subscription/getting-started, and the archive were already built (Phases 2–5). **Theme rule honoured:** customer ordering screens stay **light** ("glass day"); the dashboard/driver/admin/landing are dark. No new migration.
- **Repository audit fixes applied (FIX 0–4).** FIX 1 (revenue bug): item **options are now orderable end-to-end** — `menu_payload` includes option groups (migration 0025), the customer item-sheet renders them (single/multi, required enforced, live price), the cart/request carry option ids, and the order route re-reads + re-prices options from the DB (never trusting a client delta), enforces required/max_select, and writes them to `options_snapshot`; the orders dashboard + kitchen show them. Pricing was extracted into a pure `priceLines()` in `orders.ts`. FIX 2: the **Cairo font is self-hosted** (`next/font/local`, woff2 in `src/app/fonts/`) — the build no longer calls Google Fonts. FIX 3: **Vitest unit tests** for the money paths (`tests/`, `npm test`) + a CI workflow (`.github/workflows/ci.yml` — **must be committed by the owner; the agent's token lacks `workflow` scope**). FIX 4: `db:types` reads `SUPABASE_PROJECT_ID` from `.env.local` (`scripts/gen-types.mjs`); stray `.gitkeep`s removed; README updated. FIX 5 (DB-backed rate limiter) deliberately deferred until real abuse appears.
- **REDESIGN_V2_SPEC prompt R1 applied (colour system + dark/light).** The palette is now **warm stone neutrals + a single deep-emerald accent**, designed light *and* dark (§1–2), replacing the old Tigris-teal/river-night tokens. Tokens live in `globals.css` (`:root, .light` = light, `.dark` = dark; added `--success`/`--warning`/`--danger`/`--accent-wash` + `@theme` mappings; light-mode brand deepened to emerald-700 `#047857` so white-on-brand and accent text clear the spec's own 4.5:1 — the spec's `#0a8f64` failed at ~4.1:1). Elevation rule: shadow tokens in light, `none` in dark (§1.2). **`next-themes` is wired** (`ThemeProvider` at root, `attribute="class"`, default **light** for public/customer surfaces, system-aware, persisted, `suppressHydrationWarning`; the light class is a neutral `theme-light` marker so it never suppresses a nested staff `.dark` scope). **Staff surfaces keep their own dark default** via `StaffThemeScope` + a dedicated `useDashboardTheme` store (independent of the public preference so "dark" stays the staff default), with a **Sun/Moon toggle in the dashboard header**; the old `.dark.dj-dashboard` teal tint is retired and `.dj-dashboard` now only carries the `@media print` reset. Per-mode `theme-color` meta + manifest colours updated. Custom `dark` variant is `&:is(.dark *):not(.light *)` so a forced-light scope wins over a dark root. Contrast verified ≥4.5:1 on all 26 text pairs both modes; `tsc`/`eslint`/`test` (31)/`build` all green. **No migration.** The customer-surface theme toggle is deferred to **R3** (which rebuilds the customer menu header).
- **REDESIGN_V2_SPEC prompt R2 applied (logo & icons).** The real **river-and-bowl mark** (§3) ships as [`LogoMark`](src/components/brand/logo-mark.tsx) (`tile` = emerald tile + white mark, `plain` = `currentColor` on coloured surfaces, both token-driven) and a [`Wordmark`](src/components/brand/wordmark.tsx) lockup (mark + «دجلة» + letter-spaced `DIJLA`). Placeholders replaced: `layout/brand.tsx` now delegates to `Wordmark`; the `Waves` icon in the auth shell and landing header is now the mark. `scripts/generate-icons.mjs` rewritten to draw the mark in emerald `#047857`/white and regenerated **all PWA icons** (192/512, maskable 192/512 with 12% safe padding, apple-touch 180, favicon 32, `logo.svg`) via `npm run icons`. Added [`src/app/opengraph-image.tsx`](src/app/opengraph-image.tsx) (`next/og`, mark + Latin `DIJLA`, prerenders static; Arabic name/pitch ride in the existing OG title/description metadata — satori has no Arabic face and the Cairo woff2 can't load, so the image stays Latin). `tsc`/`eslint`(0 err)/`build` green. **No migration.**
- **REDESIGN_V2_SPEC prompt R3 applied (customer menu v2 — the visual rebuild).** Both customer routes (`/t/[qr_token]`, `/r/[slug]`) now share new chrome: [`MenuHeader`](src/components/customer/menu-header.tsx) (gradient cover §4.1 via `--brand`→`--brand-deep`, floating restaurant card, open/closed status dot, context pill «طاولة N» for dine-in, language pill + the **customer theme toggle** deferred from R1 — [`CustomerThemeToggle`](src/components/theme/customer-theme-toggle.tsx), next-themes) and [`MenuBrowser`](src/components/customer/menu-browser.tsx) (instant **search** §4.2 on name/description, **sticky scroll-spy category bar** §4.4 via IntersectionObserver, photo **card rows with quick-add** §4.5 — quick-add adds straight to cart unless the dish has a required option group, then it opens the sheet). Floating **cart pill** §4.7 ([`cart-bar`](src/components/customer/cart-bar.tsx), count badge + total + dir-aware arrow). The bottom-sheet item detail (§4.6) already existed from FIX 1. **Bug fixed in passing:** both views' `onAdd` dropped the 4th `options` arg, so **dine-in and delivery silently lost selected options** (and a required-option dish would be rejected server-side); now options flow into the cart, the cart line shows them, and display subtotals include the deltas (`price + extra`). `menu-list.tsx` retired. `tsc`/`eslint`(0 err)/`build`/`test`(31) green. **No migration.** **Deferred with clear reasons:** the §4.3 "most ordered" carousel (needs a popularity source — deliverable via a server-side aggregate in the page, no migration, on request); the tag/price **filter chips** → **R4** (needs migration `0026` tags); **cuisine + avg-prep** in the status line (no data until R4/R5); trilingual content switch → **R9** (`0029`). **R4–R12 still pending.**
- **REDESIGN_V2_SPEC migration `0026` applied + tag/prep entry built (R6 §7.3 slice).** Migration `0026_menu_tags_prep_time` added `menu_items.tags text[]` + `prep_minutes int` (+ GIN index) and extended `menu_payload` to carry them. The menu-builder **item dialog now sets tags and prep time**: a known, app-validated tag set (`src/lib/menu-tags.ts` — نباتي/حار/اختيار الشيف; "الأكثر طلباً"/"جديد" stay data-derived) as toggle chips, plus an optional prep-minutes field (clamped ≤240). Server actions `createItem`/`updateItem`/`duplicateItem` persist and re-validate them (`sanitizeTags`). Types hand-added to `types.ts` (run `npm run db:types` to canonicalise). This unblocks **R4** (filter/badges by tag & prep) and feeds the customer ETA — customer-side **display/filtering of tags is R4** (not built yet). `tsc`/`eslint`(0 err)/`build`/`test`(31) green.
- **REDESIGN_V2_SPEC prompt R4 applied (customer search & filtering).** No migration — built on `0026`. The customer menu gained a **filter panel** ([`FilterSheet`](src/components/customer/filter-sheet.tsx)): category chips, tag chips (نباتي/حار/اختيار الشيف), **price bands derived from the restaurant's own price distribution** (terciles, `derivePriceBuckets`), and prep-time thresholds (only those that actually split the menu). The apply button shows the **live match count before applying** (draft filters; the menu behind doesn't move until apply); a **clear-all** resets. Filter state lives in the **URL query** (`?cat=&tag=&price=&prep=`, filters only, **no PII**) via `history.replaceState` (no server round-trip / re-fetch); it survives refresh and is shareable. Active filters render as **removable chips** above the list; the filter button carries an active-facet badge; empty results show a friendly clear-filters state. Item rows now show **inline tag badges**. Pure logic in [`customer-filters.ts`](src/lib/customer-filters.ts) (`applyFilters`/`countMatches` — the count on the button and the rendered list call the same function, so they can't drift). tags AND-combined, price/prep single-select. Search (R3) composes with filters. **Deferred:** the "hide sold-out" filter → **R5** (`0027`); the "most ordered" carousel still needs a popularity source. `tsc`/`eslint`(0 err)/`build`/`test`(31) green. **R5–R12 pending.**
- **REDESIGN_V2_SPEC prompt R5 applied (sold-out handling + next-day auto-restore).** Migration `0027_sold_out_autorestore` added `menu_items.sold_out_at` + `auto_restore` (default true), a service-role `restore_sold_out(uuid)` (Asia/Baghdad service-day boundary), and **rebuilt `menu_payload` to stop hiding sold-out items** and carry `is_available`. Customer side: a sold-out dish now shows **dimmed + «نفد اليوم — يرجع غداً» badge**, is untappable, has no quick-add, and drops out of the orderable cart set (stale lines read as unavailable). **Security:** the order route already filtered `is_available && category.is_active` into pricing, so a shown-but-sold-out item still fails server-side — no route change needed. Dashboard: the existing one-tap card toggle now stamps/clears `sold_out_at`; a **«نفد بشكل دائم»** checkbox in the item dialog sets `auto_restore=false`; and a **bulk select mode** (`setItemsAvailable`) marks several sold-out/available at once. Auto-restore runs **lazily on menu load** via `restore_sold_out` (fired best-effort from both customer pages' server context; one-request lag by design, no pg_cron). The customer **hide-sold-out filter** (deferred from R4) is now in the filter panel (default off). `tsc`/`eslint`(0 err)/`build`/`test`(31) green. **R6–R12 pending.**
- **REDESIGN_V2_SPEC prompt R6 — paste-lines quick-add applied (§7.4); rest of R6 pending.** No migration. A **«لصق أسطر»** dialog ([`paste-lines-dialog.tsx`](src/components/menu/paste-lines-dialog.tsx)) lets an owner paste a whole menu — one dish per line, price at the end (`كباب عراقي 12000`) — with a **live parsed preview** before committing; `createItemsFromLines` bulk-inserts name+price (capped 100). The parser ([`menu-lines.ts`](src/lib/menu-lines.ts)) folds **Arabic-Indic/Persian digits**, strips thousands separators, tolerates dash/colon, and takes the *trailing* number as price (7 unit tests, `tests/unit/menu-lines.test.ts`). Triggered from every category footer + the uncategorised section. **§7.3 detail panel is effectively done** (the item dialog already has photo, secondary name, price, prep, tags, options, duplicate, and the sold-out override). **§7.6 side-by-side live preview done** ([`live-preview-button.tsx`](src/components/menu/live-preview-button.tsx)): the «معاينة» action opens the real customer route in an **iframe side panel** on wide screens (full-screen on phones), with refresh + open-in-new-tab (no frame-blocking headers exist, so the same-origin iframe renders). **§7.1 photo-first card grid done** ([`item-list.tsx`](src/components/menu/item-list.tsx) rebuilt): 2-col cards mirroring the customer view — photo header (grayscale when sold out), a sold-out overlay badge, owner-set tag badges, price, one-tap availability Switch, and edit/duplicate/delete. All prior behaviours preserved (reorder, bulk-select, sold-out styling). Cards also show **«N أحجام · N إضافات» meta badges** (option counts folded from an `option_groups(item_id, max_select, options(count))` query in the page, threaded as `optionCounts` through CategoryList → CategorySection → ItemList). **Reorder stays arrow-based** (no `@dnd-kit` added mid-context; §D.1 keeps the buttons regardless — drag is a deliberate later add). **§7.2 bulk photo drop done** ([`bulk-photo-dialog.tsx`](src/components/menu/bulk-photo-dialog.tsx)): a dashed drop zone takes many images, **auto-matches each to a dish by filename** (pure, unit-tested [`photo-match.ts`](src/lib/photo-match.ts) — normalize + contains + Levenshtein, 7 tests), a review list lets the owner reassign/skip per file, then it uploads one at a time (reusing the single-image compress→storage→public-URL path) and persists via a new `setItemImage` action (cleans the replaced file). **Caveat:** the drag-drop + multi-upload UX is build/type/test-green but was **not runtime-tested** (no browser here) — worth an eyeball on a real device. **R6 is now COMPLETE (§7.1–7.6).** `tsc`/`eslint`(0 err)/`build`/`test`(**45**) green. **R7–R12 pending.**
- **REDESIGN_V2_SPEC prompt R7 applied (menu layout templates).** Migration `0028_menu_layout` added `restaurants.menu_layout` (`auto`|`grid`|`list`|`featured`, default `auto`). [`MenuBrowser`](src/components/customer/menu-browser.tsx) now renders three layouts — **`list`** (compact rows, the prior look), **`grid`** (2-col photo cards), **`featured`** (grid with the first item of the first section spanning both columns as a hero) — sharing one set of render helpers so behaviour never drifts. **`auto` is resolved from photo coverage** (≥50% of items have a photo → grid, else list) by a pure, unit-tested [`resolveMenuLayout`](src/lib/menu-layout.ts) (4 tests). The setting is **read server-side** from the restaurant row (added to both customer pages' admin read) and threaded page → MenuView/DeliveryView → MenuBrowser. A **Settings card** ([`menu-layout-form.tsx`](src/components/settings/menu-layout-form.tsx)) picks the layout optimistically via a new `setMenuLayout` action (validates against the known set, revalidates the public routes). `tsc`/`eslint`(0 err)/`build`/`test`(**49**) green. **R8–R12 pending.**
- **REDESIGN_V2_SPEC prompt R8 applied (restaurant QR & storefront poster).** No migration — reuses the existing `qrcode` infra (`qrSvg`/`qrPng`, added `restaurantUrl(slug, origin)`). On `/dashboard/tables` the delivery-link row gains a **«رمز المطعم»** dialog ([`restaurant-qr.tsx`](src/components/tables/restaurant-qr.tsx)): the `/r/[slug]` QR on a **white padded backing** (dark modules on white, never on the dark dashboard), a **PNG download** (new [`/api/restaurant/qr`](src/app/api/restaurant/qr/route.ts) route, read through the caller's session so RLS scopes it to their own restaurant), and links to the poster/stickers. New print route [`/dashboard/tables/poster`](src/app/dashboard/tables/poster/page.tsx): an **A4 storefront poster** (name + large QR + «اطلب أونلاين» + short link) and a **sticker sheet** (`?mode=stickers`, a 3-col grid of the same QR) — both dark-on-white with an A4 `@page` and print reset, and a localhost-origin warning so nobody prints an unscannable code. **Caveat:** printed-QR scanning was not verified here (no printer) — worth confirming one scans. `tsc`/`eslint`(0 err)/`build`/`test`(49) green. **R9–R12 pending.**
- **REDESIGN_V2_SPEC prompt R9 applied (trilingual menu content).** Migration `0029_menu_third_language` added `menu_items.name_fa`/`description_fa` and rebuilt `menu_payload` to carry **all three languages' name + description** (Arabic base, English `name_secondary`/`description_secondary`, Persian `_fa`). A **content-language axis** independent of the UI locale: [`useContentLang`](src/lib/hooks/use-content-lang.ts) store (persisted, default `ar`) + a header pill [`ContentLangSwitcher`](src/components/customer/content-lang-switcher.tsx) (ع | En | فا) that swaps the shown names/descriptions and, for ع/En, flips the UI locale too (فا keeps RTL Arabic chrome, no Persian UI dict). `pickName`/`pickDescription` in [`menu.ts`](src/lib/menu.ts) fall back to Arabic when a translation is missing (never an empty name); the customer **search matches the active language**. Menu builder: the item dialog now has **English + Persian name and description fields** with a **coverage indicator** (ع/En/فا), persisted/duplicated by the item actions. **Deferred polish:** the cart lines and order snapshot still show the canonical Arabic name (menu display is localized; cart is not) — worth localizing the cart line later; option-group names have no translation columns (out of R9 scope). `tsc`/`eslint`(0 err)/`build`/`test`(49) green. **R10–R12 pending.**
- **REDESIGN_V2_SPEC prompt R10 applied (Web Push — code-complete, needs device test).** Migration `0030_push_subscriptions` (table + `own_push_subscriptions` RLS). **Server send** ([`lib/push/send.ts`](src/lib/push/send.ts)): configures web-push from `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` (no keys → silent no-op), `sendNewOrderPush` reads a restaurant's subs (service role) and pushes, **pruning 404/410**. Fired from the order route via **`after()`** so it never adds latency to or fails the order. **Client** ([`push-setup.tsx`](src/components/pwa/push-setup.tsx)): a slim staff-chrome banner — enable prompt → permission + `pushManager.subscribe` + `savePushSubscription` (RLS `user_id = auth.uid()`, upsert on endpoint), a **persistent "blocked" warning**, **iOS install instructions** when iOS non-standalone (push needs an installed PWA, 16.4+), and a disable control. **SW** ([`public/sw.js`](public/sw.js)): `push` shows an Arabic notification (order #, type, context, total), `notificationclick` focuses/opens `/dashboard/orders`; the no-HTML-cache rule is untouched. Ambient `web-push` types added (`src/types/web-push.d.ts`); `.env.example` documents the VAPID vars. **Owner has set the VAPID keys (local + Vercel) and applied `0030`.** ⚠️ **NOT device-verified** — the whole point is "arrives with the page closed", which only a real phone proves (Android Chrome; iOS requires the installed PWA). **Deferred:** driver-app subscription (assignment pushes — staff/new-order only for now); the §11 3-minute WhatsApp/SMS fallback (needs PHASE_6 module B — left unbuilt, no hook yet). `tsc`/`eslint`(0 err)/`build`/`test`(49) green. **R11 (verify) + R12 (memory) remain; then the U-series.**
- **UX_IMPROVEMENTS_SPEC `U7` applied (dashboard keyboard shortcuts, §E).** No migration. The live orders board gained keyboard control: **`j`/`k` or `↑`/`↓`** move a selection highlight through the flattened active cards (with scroll-into-view + a `ring-brand` ring), **`Enter`** advances the selected order via `nextStatus(status, type)` through the same `setOrderStatus` the card buttons use (so the 10-second undo still applies), **`Esc`** clears, **`?`** opens a discoverable shortcuts overlay (also a `Keyboard` button in the header). **Guarded**: ignores keys while an input/textarea/contenteditable is focused (Arabic typing is never intercepted) and while a modifier is held; nav is vertical so RTL is moot; every shortcut has a visible button equivalent. (`/`-focus-search is n/a — the board has no search.) Also applied **U6 §D.3**: each lane header shows its **oldest order's age** (reusing the live `ElapsedTimer` with the warn/danger thresholds), so a backing-up lane is obvious at a glance. `tsc`/`eslint`(0 err)/`build`/`test`(54) green.
- **UX_IMPROVEMENTS_SPEC `U3` applied (report insights, comparisons, charts).** No migration — built on the existing report RPCs. **§B.1 period comparison:** the reports page now fetches the **equivalent previous window** (same span, immediately before) and each KPI tile shows a signed % change (▲ brand / ▼ muted), zero-baseline-safe (no infinity) via a pure `percentChange`. **§B.2 plain-language insights:** [`report-insights.ts`](src/lib/report-insights.ts) `selectInsights` derives up to **4 Arabic sentences** — busiest 3-hour window + its order share, best seller, order growth/drop vs previous, delivery share — each gated on enough data (5 unit tests); rendered above the charts. **§B.3:** top-items rows gained **proportional revenue bars** (peak-hours + type bars already existed). `tsc`/`eslint`(0 err)/`build`/`test`(**54**) green.
- **UX_IMPROVEMENTS_SPEC started — U-prompt `C.1` (driver screen wake lock) applied.** No migration. Extracted a reusable [`useScreenWakeLock(enabled)`](src/lib/hooks/use-screen-wake-lock.ts) (feature-detected, re-acquires on `visibilitychange`, degrades silently) — the **driver app now holds the screen awake while a delivery is in hand** (`orders.length > 0`), and the kitchen board was **de-duplicated** onto the same hook. `tsc`/`eslint`(0 err)/`build`/`test`(49) green. **U5 §C.2 (driver proximity ordering) also applied** — a pure, unit-tested [`haversineKm`](src/lib/haversine.ts) (3 tests) orders a driver's deliveries by distance from the **restaurant** (read `lat`/`lng` from the driver's own restaurant row, threaded to `DriverOrderList`); each card shows «≈ N كم» and a **Nearest/Oldest toggle** appears only with >1 pinned delivery (driver live-position is a later option). **U2 §A.1/A.2 (admin growth charts) also applied** — a pure, unit-tested [`admin-charts.ts`](src/lib/admin-charts.ts) (`newRestaurantsPerMonth`/`cumulativeRestaurantsPerMonth`/`mrrPerMonth`, 3 tests) derives 12-month series **from the existing `admin_restaurants` RPC** (created_at + start/end/amount/status — no new RPC). [`GrowthCharts`](src/components/admin/growth-charts.tsx) renders a **new-restaurants-per-month bar chart with a cumulative toggle** and an **MRR curve** with the current value + MoM change, both inline SVG, single brand colour, token-driven; added to `/admin/growth`. MRR is an approximation (paying subs active per month), fine for a trend line. **Done so far in the U-series:** C.1 (wake lock), U3 (report insights/comparisons/charts), U7 (keyboard shortcuts) + U6 §D.3 (lane oldest-age), C.2 (proximity), U2 §A.1/A.2 (growth/MRR charts). **U1 (churn workflow) + U2 §A.4 (cohort retention) also applied** — migration `0032_admin_cohorts` (functions `admin_dormant_restaurants(days)` + `admin_cohort_retention()`, both `is_platform_admin()`-gated, and the `admin_outreach` table with admin-only RLS). On `/admin`, a [`ChurnRiskPanel`](src/components/admin/churn-risk-panel.tsx) lists restaurants quiet 7+ days with **risk tiers** (7–13 warning, 14+ critical), **tap-to-call**, and a **«تسجيل متابعة»** dialog (`logOutreach` → `admin_outreach`, so the same one isn't chased twice, last-contacted shown); the rows are built by a pure, unit-tested [`buildChurnRows`](src/lib/churn.ts) (2 tests — first-week + cancelled excluded, sorted, latest outreach). On `/admin/growth`, [`CohortRetention`](src/components/admin/cohort-retention.tsx) renders the signup-month × months-since retention table with **colour-scaled cells** (`color-mix` on `--brand`). `tsc`/`eslint`(0 err)/`build`/`test`(**62**) green. **U4 (CSV export + item profit) also applied** — migration `0031_item_cost` (optional `menu_items.cost`, never in `menu_payload`). **§B.5:** the item dialog gained an optional **cost** field; the reports page joins sold-item totals to costs **by name** (top-items RPC has no id) via a pure, unit-tested [`itemProfit`](src/lib/report-profit.ts) (2 tests) and shows a **«الأعلى ربحاً» ranking by total profit** (not revenue) with per-item margin, a "no costs yet" empty state, and a count of items missing a cost. **§B.4:** an [`ExportCsvButton`](src/components/reports/export-csv-button.tsx) downloads the range's per-item totals as **UTF-8-BOM CSV** (Excel-safe Arabic), built in the browser — no order rows cross over. `tsc`/`eslint`(0 err)/`build`/`test`(**64**) green. **U5 §C.3 (driver "arrived") also applied** — migration `0033_driver_arrival` (`orders.driver_arrived_at` + `get_order_status` now returns a PII-free `arrived` boolean). A new `markArrived` driver action (same `assignedOrder` ownership gate, whitelist = that one column, only from `out_for_delivery`) fires from a **«وصلت» button** on the driver card; once stamped the button becomes a «وصل» tick. The customer tracker ([`order-tracker`](src/components/customer/order-tracker.tsx)) shows **«السائق وصل — استلم طلبك»** when the poll reports `arrived`. `driver_arrived_at` threaded through `DriverOrder` + both `DRIVER_SELECT`s. A clean WhatsApp/SMS hook is noted (PHASE_6 module B, unbuilt). `tsc`/`eslint`(0 err)/`build`/`test`(64) green. **U5 complete (C.1/C.2/C.3).**
- **UX_IMPROVEMENTS_SPEC `U6` applied — the last U-item; the U-series is now COMPLETE.** **§D.1 drag-to-advance:** added `@dnd-kit/core`; the live orders board wraps its three lanes in a `DndContext` — each lane is a droppable, each card a draggable (pointer sensor, 8px activation distance so a tap on a card button stays a click, only pointer `listeners` spread so the wrapper never becomes a `role="button"` around the card's own buttons). Drop logic is a pure, unit-tested [`resolveDrop`/`laneOfStatus`](src/lib/board-drag.ts) (`accepted`→preparing, `out_for_delivery`→ready; ready-lane entry is `ready`, never `out_for_delivery`, so the driver hand-off stays a deliberate press): **forward-only** (backward → toast «لا يمكن إرجاع الطلب للخلف بالسحب»), same-lane is a no-op, and the move routes through the existing `useOrderMove` so the **10-second undo still applies**. Buttons and the j/k/Enter keyboard path are untouched (drag is additive; RTL is moot — lanes are ordered, not sided). 7 unit tests (`tests/unit/board-drag.test.ts`). **§D.2 learned prep time:** migration `0034_learned_prep` adds a `security definer` `learned_prep_minutes(p_days=30, p_min_sample=5)` RPC — the **median** wall-clock minutes from `new`→`ready` (first of each, so a reverted mis-tap doesn't count) over the window, attributed to each item via `order_items.menu_item_id`, `having count >= min_sample`, **scoped to `current_restaurant_id()`** (safe inside definer — `auth.uid()` still reads the caller's JWT). The menu page reads it and attaches `{median, sample}` per item; the item dialog shows **«متوسطك الفعلي N دقيقة — اضغط للاستخدام»** under the prep field as a **tap-to-apply suggestion** (never silently overwrites the typed value). The customer ETA is reached transitively — applying writes `prep_minutes`, which already flows into `menu_payload` and drives the customer prep filter; a standalone auto-injected ETA badge is deliberately **deferred** (no customer ETA display exists yet, and auto-injection would contradict "never overwrite"). §D.3 (lane oldest-age) was already done. `tsc`/`eslint`(0 err)/`build`/`test`(**71**) green. **The entire UX_IMPROVEMENTS_SPEC U-series (U1–U9) is now applied.**
- **Migrations applied through `0034`.** Migrations are pasted into the Supabase SQL editor by the owner (the agent has no DDL access); each is idempotent/re-runnable. Unit tests: `tests/` (Vitest, pure, `npm test`). Integration scripts live in the gitignored `coverage/` dir (node `.mjs`, run against real Supabase, self-cleaning).
- **Payments:** COD (cash) only — electronic payment (Phase 6) deliberately deferred by the owner.
- **The real remaining step is a pilot**, not more building: put Dijla in a real Najaf restaurant. Personal setup still pending: set `NEXT_PUBLIC_SUPPORT_PHONE` in Vercel; real-device test of driver GPS/PWA/order sound; optional custom domain.
- Full plan: see `DIJLA_PROJECT_PLAN.md`.

## Docs

- **`MASTER_INDEX.md` — read this first.** It is the single source of truth for routing and conflict resolution: which file governs which screen (§4), the precedence rule when two docs disagree (§2), the corrections/resolved conflicts (§3), the migration ledger (§5), and the non-negotiable security rules (§6). If any two documents disagree, `MASTER_INDEX.md` decides.
- **Specs (these supersede the matching tasks in the `PHASE_*_BUILD` files — build from the spec, not the phase task):**
  - `AUTH_UI_SPEC.md` — auth & onboarding (supersedes tasks 1.1–1.2).
  - `MENU_BUILDER_SPEC.md` — menu builder + tables/QR (supersedes 1.5/1.6/2.2/2.3).
  - `ORDERS_DASHBOARD_SPEC.md` — live orders dashboard (supersedes 2.6).
  - `DRIVER_REPORTS_ADMIN_SPEC.md` — driver app, reports, admin (supersedes 4.4/4.5/4.8, 5.1/5.2/5.4/5.5).
  - `REMAINING_SCREENS_SPEC.md` — delivery checkout, landing, supporting screens (supersedes 3.2/3.3; covers 1.7/2.8/5.10).
  - `REDESIGN_V2_SPEC.md` — **highest authority on visuals** (colour tokens, dark/light, logo, customer menu v2, filtering, sold-out, menu builder v2, layouts, restaurant QR, trilingual, push). Supersedes `AUTH_UI_SPEC §1`, `MENU_BUILDER_SPEC §1–6`, `PHASE_2 2.4`, `PHASE_3 3.1`, `ORDERS_DASHBOARD §4`. **Prompts `R1`–`R10` applied** (theme, logo/icons, customer menu v2, search/filter, sold-out, menu builder v2, layouts, restaurant QR/poster, trilingual, Web Push) — see Current status; migrations `0026`–`0030` all applied. **`R11` (verify) pass complete; `R12` (memory) kept current inline.** REDESIGN_V2 is functionally complete — remaining real-world gates: **device-test Web Push** (R10) and **print-test a QR** (R8). The **U-series** (`UX_IMPROVEMENTS_SPEC`, migrations `0031`–`0033`) is the next body of work.
  - `UX_IMPROVEMENTS_SPEC.md` — admin charts + churn workflow, report insights, driver upgrades, orders-board drag/learned-prep, keyboard shortcuts. **Extends** (doesn't replace) `DRIVER_REPORTS_ADMIN_SPEC §A/B/C` and `ORDERS_DASHBOARD §2–3`. **Saved but not yet implemented** — migrations `0031`–`0033` pending; prompts `U1`–`U9`.
  - `PHASE_0_1…PHASE_5_BUILD.md`, `PHASE_6_BUILD.md`, `PHASE_7_PLAYBOOK.md` — original phase plans.
- Antigravity IDE: https://antigravity.google
- (Portable `AGENTS.md` spec works across Antigravity, Cursor, and Claude Code.)

<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->
